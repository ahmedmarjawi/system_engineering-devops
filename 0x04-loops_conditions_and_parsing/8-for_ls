#!/usr/bin/env bash
# This script lists files in the current directory and displays only the part after the first dash

for file in *-*; do
  # Skip hidden files and ensure it's not a directory
  [ -e "$file" ] || continue
  name="${file#*-}"
  echo "$name"
done
